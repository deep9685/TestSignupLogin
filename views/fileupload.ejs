<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>File Upload</title>
</head>
<body>
    <form id="uploadForm" enctype="multipart/form-data">
        <input type="file" name="files" multiple>
        <button type="submit">Upload Files</button>
    </form>

    <!-- <form method="post" action="/test">
        <input type="number" name="percentField_1" placeholder="Enter percent field 1">
        <input type="number" name="percentField_2" placeholder="Enter percent field 2">

        <button type="submit">Submit</button>
    </form> -->

    <script>
        document.getElementById('uploadForm').addEventListener('submit', async function(event) {
            event.preventDefault();
            const formData = new FormData(this);

            try {
                const response = await fetch('/login/upload/2', {
                    method: 'POST',
                    body: formData
                });

                const result = await response.json();
                if (response.ok) {
                    console.log('Files uploaded successfully:', result.files);
                } else {
                    console.error('Upload failed:', result.message);
                }
            } catch (error) {
                console.error('Error uploading files:', error);
            }
        });
    </script>
</body>
</html>
